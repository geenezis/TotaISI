.container-fluid.content-container.bg-grey
  .form-container
    .form-title
      %h1 Edit Project
    = simple_form_for @project do |f|
      .form-inputs
        = f.input :name, required: true, autofocus: true
      .form-actions
        = f.button :submit, "Save project", class: "btn-primary btn-lg btn-form"
